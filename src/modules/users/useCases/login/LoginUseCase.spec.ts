describe('LoginUseCase', () => {
  // it('should login a use with valid credentials', () => {})
  // it('should give a valid refresh token after successful login', () => {})
  // it('should persist the generated refresh token to the database', () => {})
  // it('should give a valid access token after successful login', () => {})
  // it('should throw InvalidCredentialsError if user does not exist', () => {})
  // it('should throw InvalidCredentialsError if the password is incorrect', () => {})
})
